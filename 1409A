#include <bits/stdc++.h>
using namespace std;
#define ll long long

int main()
{
    ll tc;
    cin >> tc;
    
    while(tc--) {
        ll a, b;
        cin >> a >> b;
        
        int d = abs(a - b);
        
        ll cnt = (d+9)/10;
        cout << cnt << "\n";
    }
    return 0;
}
